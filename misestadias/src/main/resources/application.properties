server.port=8080

# --- DATASOURCE (Oracle Autonomous Cloud + Wallet) ---
# Usa el TNS name directamente ? el Wallet resuelve la dirección
spring.datasource.url=jdbc:oracle:thin:@mahmf_high
spring.datasource.username=ADMIN
spring.datasource.password=MAHMFintegradora5
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- WALLET: ruta a la carpeta donde descomprimiste el Wallet ---
# Spring Boot la pasa a Oracle JDBC como propiedad del sistema
spring.datasource.hikari.data-source-properties.oracle.net.wallet_location=C:\\Users\\omarh\\OneDrive\\Documents\\INTEGRADORAV\\Wallet_MAHMF
spring.datasource.hikari.data-source-properties.oracle.net.tns_admin=C:\\Users\\omarh\\OneDrive\\Documents\\INTEGRADORAV\\Wallet_MAHMF


spring.jpa.hibernate.ddl-auto=update

# Dialecto para Oracle 12c+ (compatible con 19c, 21c)
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Muestra el SQL generado por Hibernate en consola (útil para debug)
spring.jpa.show-sql=true

# Formatea el SQL en consola para que sea legible
spring.jpa.properties.hibernate.format_sql=true

# --- NOMBRE DE LA APP ---
spring.application.name=misestadias

# --- JWT ---
jwt.secret=clave-super-secreta-de-al-menos-32-caracteres-utez-2026
jwt.expiration=86400000

# Deshabilita open-in-view (evita el warning en consola)
spring.jpa.open-in-view=false

# --- EMAIL (Gmail SMTP) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mahmf.verificacion@gmail.com
spring.mail.password=mcgkqyedlrztrmnu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

logging.level.org.springframework.security.config...=ERROR